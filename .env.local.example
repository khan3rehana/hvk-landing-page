# Base URL of the hvk-backend API (see the `backend` branch).
# Must be reachable from the browser, and the backend's CORS_ORIGINS must include
# this app's origin (e.g. http://localhost:3000 in local dev).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
